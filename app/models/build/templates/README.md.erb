# <%= gem.name %>

> The Bower package inside a gem

This gem was automatically generated. You can visit [<%= Rails.configuration.x.hostname %>](https://<%= Rails.configuration.x.hostname %>) for more information.

## Usage

Add rails-assets source block to your `Gemfile`:

```ruby
source "https://<%= Rails.configuration.x.hostname %>" do
  gem <%= gem.name.dump %>
end

```

Then, import the asset using Sprockets’ `require` directive:

```js
//= require <%= bower_component.name.dump %>
```
