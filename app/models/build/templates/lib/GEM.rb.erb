require "<%= gem_component.name %>/version"

<% gem_component.dependencies.each do |gem| %>
require <%= gem.name.dump %>
<% end %>

if defined?(Rails)
  module <%= gem_component.module %>
    class Engine < ::Rails::Engine
      # Rails -> use vendor directory.
    end
  end
end
